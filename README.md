# Student-Record-Management-System-C-
This is a console-based student database application written in C. It provides role-based access control with three user types:  Admin  Staff  Guest  The program reads user login credentials from an external file and provides different menu options depending on the logged-in user's role. The system manages student details stored in a text file.
